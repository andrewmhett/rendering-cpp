#include <vector>

class Object{
public:
  //Object(vector<double[3]> vertices, vector<double[3]> normal_vectors, vector<int[2]> faces):
    //load in vertices and normal vectors, number them, then compute faces
};
